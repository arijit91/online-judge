extends layout

block content
  if (!grade)
      div.row
        h4 #{username} has not yet been graded for this problem.
  else 
      div.row
        h4 #{username} has already been graded for this problem for a
      score of #{score}.

  if (num_subs == 0)
      div.row
        h4 #{username} has not yet made any submissions for this problem.
        hr
  else
      div.row
        h4 Number of submissions made: #{num_subs}
        p Below are the submissions sorted by how recent they are.
        hr

      - for (var i = 0; i < num_subs; i++)
            div.row
                h4 Submission Date: #{submission_date[i]}
            div.row
                textarea(rows="10")= files[i]
            hr
            


  //h3 Grade #{user} for #{problem}!
  //table.table.table-striped
  //  th Name
  //  th Login Name
  //  - for (var i = 0; i < users.length; i++)
  //      tr
  //              td 
  //                  a(href='/admin/problem/grade/#{problem.code}/#{users[i].username}') #{users[i].name}
  //              td #{users[i].username}
